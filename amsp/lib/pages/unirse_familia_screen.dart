import 'package:flutter/material.dart';

class UnirseAFamiliaScreen extends StatelessWidget {
  const UnirseAFamiliaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Unirse a una familia')),
      body: const Center(child: Text('Pantalla para unirse a una familia')),
    );
  }
}
